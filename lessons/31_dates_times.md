# Dates and Times
* Create a `Timestamp` with:
  * `pd.to_datetime('now')`
  * `pd.to_datetime(s)` for some Series `s`
    * Pandas is quite clever converting strings to dates
* Extract parts of a Timestamp with attributes like `month` and `hour`
* Create `timedelta` with:
  * Subtracting Timestamps
  * `pd.to_timedelta('2 days 3 hours`)
* Challenge: If the new Congress introduces articles of impeachment on the day they are inaugurated, how long to we have to wait?
* Work through notebooks
