# Cleaning Data
* Get `skycrapers.csv` from the book's data files
* For each of the following tasks:
  * Everyone tries to answer it simultaneously
  * Once three people have answers, we'll compare them
    * If they agree, they're probably right
  * Then reveal the solution
  * On to the next question, but nobody can answer more than once
* How many rows are in the dataset?
* How many `NaN`s are in the dataset?
* Fix those `NaN`s
* Make a scatter plot showing the latitudes and longitudes of all the buildings
* What are the buildings north and west of the contiguous USA?
* How many buildings have latitude 0? How many have longitude 0?
* What cities are they in?
* Conceptually (TPS), how might we fix these if:
  * We're willing to lose those data
  * We need them to be approximately correct (right city)
  * We need them to be accurate
* Plot the locations omitting these buildings
* How many buildings have height 0?
* For each different value in `status.current`, what is the maximum height?
* What building has the most floors above ground?
* Among the completed buildings
  * Which is the tallest?
  * Which has the most floors above ground?
